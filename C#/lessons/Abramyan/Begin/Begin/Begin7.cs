namespace Begin
{
    public class Begin7
    {
        public static string Begin(int r)
        {
            double l = 2 * Math.PI * r;
            double s = Math.PI * Math.Pow(r, 2);
            return $"{l}, {s}";
        }
    }
}
